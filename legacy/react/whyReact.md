# WHY USE REACT

* It makes building modern complex user interfaces  way easier
* A client side JavaScript library
* It does that with a special syntax called JSX where we write in a declarative (describing on what you want it to do), component focused  way.